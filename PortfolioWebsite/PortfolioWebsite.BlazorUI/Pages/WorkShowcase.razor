@page "/work"
@inject IPortfolioDataService portfolioDataService;

<LoadablePage Page="page" />

@code {
    private PageInfoModel page;

    protected async override Task OnInitializedAsync()
    {
        var workShowcase = await this.portfolioDataService.GetWorkShowcaseInfoAsync();
        this.page = workShowcase.Page;
    }
}

@* TODO: Investigate why sometimes the work cards jitter. *@