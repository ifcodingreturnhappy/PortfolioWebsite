@page "/contactme"
@inject IPortfolioDataService portfolioDataService;

<LoadablePage Page="page" />

@code {
    private PageInfoModel page;

    protected async override Task OnInitializedAsync()
    {
        var contactMe = await this.portfolioDataService.GetContactMeInfoAsync();
        this.page = contactMe.Page;
    }
}